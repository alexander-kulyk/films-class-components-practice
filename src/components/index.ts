import { ErrorBoundary } from './ErrorBoundary';
import { MoviesList } from './MoviesList';
import { Layout } from './Layout';
import { Modal } from './Modal';

export { MoviesList, Layout, Modal, ErrorBoundary };
